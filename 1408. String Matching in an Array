class Solution:
    def stringMatching(self, words: List[str]) -> List[str]:
        res=set()
        for i in words:
            for j in words:
                if i in j and i!=j:
                    res.add(i)
        return list(res)
        
